pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                // sh 'npm install'
                // sh 'npm build'
                echo 'BUILD'
            }
        }
        stage("test") {
            steps {
                echo "TEST"
            }
        }
        stage("deploy") {
            steps {
                echo "DEPLOY"
            }
        }
    }
}